<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Storage permissions for image picking -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
                     android:maxSdkVersion="28" />
    <!-- Camera permission -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".RecipeNestApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        tools:replace="android:fullBackupContent"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecipeNest"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="31">

        <!-- Splash Screen (First Screen When App Opens) -->
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Legacy Splash Screen - keep for backward compatibility -->
        <activity
            android:name=".SplashScreen"
            android:exported="false" />

        <!-- Onboarding Screens -->
        <activity android:name=".OnboardingActivity" android:exported="false"/>
        <activity android:name=".OnboardingPagerActivity" android:exported="false"/>
        
        <!-- Keep original onboarding activities for backward compatibility -->
        <activity android:name=".OnboardingActivity2" android:exported="false"/>
        <activity android:name=".OnboardingActivity3" android:exported="false"/>
        <activity android:name=".OnboardingActivity4" android:exported="false"/>

        <!-- Login Screen -->
        <activity android:name=".LoginActivity" android:exported="false"/>

        <!-- Signup Screen -->
        <activity android:name=".SignupActivity" android:exported="false"/>

        <!-- Personlization Screens -->
        <activity android:name=".PersonalizationActivity" android:exported="false"/>

        <!-- Forgot Password Screen-->
        <activity android:name=".ForgotPasswordActivity" android:exported="false"/>

        <!-- Recipe Creation Screen -->
        <activity 
            android:name=".CreateRecipeActivity" 
            android:exported="false"
            android:theme="@style/RecipeCreationTheme"/>

        <!-- Main Activity (Final Destination After Onboarding/Login) -->
        <activity android:name=".MainActivity" android:exported="true" />

        <!-- Recipe Detail Activity -->
        <activity
            android:name=".RecipeDetailActivity"
            android:exported="false" />

        <!-- UCrop Activity -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- Search Activity -->
        <activity
            android:name=".SearchActivity"
            android:exported="false"
            android:theme="@style/Theme.RecipeNest" />

        <!-- My Recipes Activity -->
        <activity
            android:name=".MyRecipesActivity"
            android:exported="false"
            android:theme="@style/Theme.RecipeNest" />

        <!-- Profile Activity -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.RecipeNest" />

        <!-- Firebase Integration (if needed in the future)
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id"/> -->

        <!-- FileProvider for camera captures -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.kritavya.recipenest.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
